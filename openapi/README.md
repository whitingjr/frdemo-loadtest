# Swagger API Doc for frdemo
A docker run shell to start the swagger ui to display the frdemo api.
Run swagger-ui docker iamge with:
```
./docker-run.sh
```
Access the swagger api doc from ```http://localhost/swagger```